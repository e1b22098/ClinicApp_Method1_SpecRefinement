<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clinic.repository.TimeSlotMapper">
    <resultMap id="TimeSlotResultMap" type="com.clinic.domain.TimeSlot">
        <id property="timeSlotId" column="time_slot_id"/>
        <result property="slotName" column="slot_name"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="TimeSlotResultMap">
        SELECT time_slot_id, slot_name, start_time, end_time, created_at, updated_at
        FROM time_slots
        ORDER BY start_time ASC
    </select>

    <select id="findById" resultMap="TimeSlotResultMap">
        SELECT time_slot_id, slot_name, start_time, end_time, created_at, updated_at
        FROM time_slots
        WHERE time_slot_id = #{timeSlotId}
    </select>
</mapper>

